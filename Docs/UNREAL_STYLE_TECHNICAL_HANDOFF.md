# Unreal style proof technical handoff

2026-09-08. Scope: isolated revisioned helmet proof, no global config/C++ changes. This file retains historical architecture and USP_v001–USP_v011 evidence. The [current implementation plan](MEL17_UNREAL_STYLE_PLAN.md) governs the proposed Clay_v005 transfer; the [current checkpoint](UNREAL_STYLE_CHECKPOINT.md) governs acceptance. Old source names, schema, normal-control assumptions and Unlit experiments below are not instructions to repeat them.

## Historical compact checkpoint — USP_v011

The single isolated Unlit response diagnostic is implemented in `Tools/UnrealStyleProofResponse.py` and captured successfully in USP_v011/run_004. Five frames compare standard-lit authored, Unlit control/authored matched, and Unlit control/authored native. It is explicitly **not production accepted**: diagnostic steel has no automatic scene shadows or environment reflections. Original DefaultLit materials and saved map remain intact; dark/interior slots retain their source material.

The graph decodes the existing DX normal with a normal sampler, normalizes `lerp((0,0,1), decodedNormal, NormalStrength)`, explicitly transforms Tangent to World with the mesh TBN, and normalizes again in the response expression. It never relies on PixelNormalWS. Strength 0 therefore recovers the mesh normal: MaterialTemplate.ush documents TangentToWorld[2] as WorldVertexNormal. Every capture supplies `-DirectionalLight.GetActorForwardVector()` and verifies the dynamic material value; actual direction is (-.541675, .541675, .642788).

`Tools/MeasureUnrealResponseNormals.py` measures source BVH-visible triangle centroids with perspective area weights: active cheek N·L quartiles .458/.507/.539, roof median .842, average authored delta .027. One continuous three-value ramp uses smoothstep anchors .40/.60/.95, uniform steel tint (.29,.285,.275), and low/mid/high response multipliers (.30,.38,.48)/(.90,.83,.70)/(1.50,1.48,1.40). A restrained view-dependent Blinn term has strength .06 and exponent 32. Full formula, fitted ranges, asset paths and limitations are in response_diagnostic_definition.json and the capture receipt. Manual exposure is unchanged. No parameter grid or further HDR remap was run. The output groups warm/cool values more clearly but reads matte; critique remains pending.

Two initialization failures are preserved in USP_v011/run_002 and run_003: Transform's display pin differs from UPROPERTY Input (verified empty-pin selection addresses input 0), and CustomInput requires construction followed by property assignment. Both were caught before pixels. Initialization failures now release editor keep-alive; the successful graph uses exposed input-name introspection and asserts every link.

USP_v005 is imported and validated at35cm, preserving USP_v003 geometry while adding one bounded left-cheek normal field. `run_001` isolates Env3 control/normal and Env4 normal; `run_003` changes only Stage_v002 context, raising helmet and camera145cm, wall to400cm height, limestone albedo(.50,.42,.29). It includes matched and native ordinary PIE stills. `run_004` is a rejected amplitude diagnostic: transient dynamic material strengths[2,2,0,0], estimated source peak5.353 to10.660degrees under normalize(lerp(flat,decodedNormal,strength)). Saved instances stay atstrength1; the critic still sees soft dents. No still fidelity or motion gate has passed.

Current reproducible settings: authored Environment_v004, SourceCubemapAngle=-90, key specular .15, reference18 camera, fixed exposure, Competitive/TAA. USP_v006/run_001 contains the rim geometry baseline (matched, matte, native) under Stage_v002. USP_v006/run_002 isolates Stage_v003: opening X60 to115cm, yielding roughly195px matched and160px native limestone margin beyond the shell. USP_v007/run_001 compares feather .10 against .32 with the same geometry, stage, lighting and strength1. Both Stage3 sets contain matched/native captures and successful no-pawn/camera autoactivation verification. The sharpening remains visually modest; pending critic assessment, no WorldNormal diagnostic or motion shots were added. All captures use immutable run folders with `completed`, actual parameters and frame receipts. `-Mode save` remains deferred until a winning state is identified.

Stage material validation uses actual parameter getters. Installed UE5.8 MaterialEditingLibrary.cpp:1573 vector setter performs its update yet returnsfalse unconditionally; this caused preserved failedv005/run_002 and is now handled. Shared `Tools/UnrealStyleProofStage.py` keeps capture and open/save stage transforms aligned.

USP_v007/run_002 proves authored fields survive into the actual WorldNormal buffer. `-CaptureSet worldnormal` forces and asserts ShowFlag.VisualizeBuffer=1 and r.BufferVisualizationTarget=WorldNormal; GameViewportClient.cpp:1622 resolves the registered `/Engine/BufferVisualization/WorldNormal.WorldNormal` material. Ordinary PIE screenshots show a coherent cheek interior and narrower feather. Read-only pixel audit records mean interior RGB shift(+7.17,+2.72,+2.57), channel SD below.50 over416pixels; adjacent outside442pixels never exceed2levels and wall8000pixels remain identical. See worldnormal_analysis.json and its reproducible script. This establishes rendered transfer, without claiming exact angular accuracy from 8-bit output. Further lit-response tuning is separate.

USP_v008/run_001 successfully captures the integrated forehead return under unchanged Stage3/Env4 at strength1: matched normal, matched matte and native normal. Import height remains35cm and runtime no-pawn/camera activation checks pass. Geometry/art acceptance remains with the critic; no new environment tuning or motion has been run.

USP_v008/run_002 isolates Environment_v005's targeted lower reflection boundaries: Env4 normal matched, Env5 control matched, Env5 normal matched and Env5 normal native. Actual distinct cubemap asset paths and unchanged Stage3/reference18/angle-90/key.15 are verified in the receipt. Authored cheek fields now visibly affect the lit surface at strength1; their restraint and overall fidelity remain an artist gate. `-CaptureSet environment_response` reproduces this four-frame comparison. All previous environments and captures are preserved.

USP_v009/run_001 adds the hinge geometry baseline under unchanged Env5/Stage3. USP_v010/run_001 isolates asymmetric edge-connected cheek contours on exactly the same mesh/UVs and unchanged roughness: control/normal matched and native, four successful PIE frames. `-CaptureSet material_compare` reproduces this comparison. The large closed patches become smaller sharper marks; fidelity acceptance remains pending. The next source hinge-orientation revision must remain separate from this map experiment.

USP_v010/run_002 captures WorldNormal control/authored across the whole cheek. `Tools/AnalyzeUnrealCheekNormals.py` records the display-space mask, hashes, thresholds, components and spatial bins in whole_cheek_worldnormal_analysis.json. Of20,616 cheek pixels,20.1% differ by more than2RGB levels. After excluding control-normal discontinuities plus3px margin,17.6% of16,826 face-interior pixels differ. Broad left footprint spans x681–753,y449–524; right is much smaller. This supports broad-left transfer rather than collapse into the beauty image's tiny glint. Threshold sensitivity and 8-bit display limits are explicit; source ray audit separately finds Env5 misses the new field's reflected angles. Maps remain frozen pending a targeted environment experiment.

## Contracts

Exporter owns `ArtSource/StyleReference/MEL17/UnrealProof/USP_v001/`: normalized evaluated assembly `.blend`, triangulated FBX, 2048 DirectX tangent normal, linear packed mask (R roughness, G edge, B authored influence), manifest. Retain source unchanged. Bake parent scale and modifiers; exclude stage/light/camera; bottom-center origin, complete assembly 35 cm high. This is an explicit staging scale, not inferred anatomical measurement. Freeze triangles before tangent bake. FBX: `axis_forward=-Y, axis_up=Z`, unit scale .01, geometry numeric centimeters. Import normals+tangents, no material/texture auto-import, no collision or Nanite.

Manifest fields: schema_version, revision, source_blend, source_sha256, fbx, fbx_sha256, object_names, material_slots (index/name/base_color_linear/metallic/roughness), textures (normal and masks path/sha256/color_space/convention/channels), normalization (origin_blender/cm_per_blender_unit/height_cm), bounds_cm (min/max), triangle_count, landmarks_blender and expected_landmarks_ue_cm. Relative file paths resolve from manifest directory. Receipt records actual imported material slot names, bounds, all source hashes and effective console variables.

FBX default import uses `convert_scene=True, force_front_x_axis=False, convert_scene_unit=True`. Installed importer explicitly keeps -Y FBX forward and converts handedness by negating Y (`FbxMainImport.cpp:1526`, `FbxUtilsImport.cpp:63`). For matching -Y/Z export, convert camera/target positions with `UE = scale * (Bx-Ox, -(By-Oy), Bz-Oz)`. Verify front/ridge landmarks in engine; do not trust axis option labels alone. Source camera is orthographic and cannot be copied as perspective. Set perspective horizontal FOV from `2*atan(sensor_width/(2*focal_mm))`, and orientation with `MathLibrary.find_look_at_rotation(camera,target)`. Start from source viewpoint direction, fit distance to helmet projection, record final FOV and transform.

## Minimal implementation

`Tools/ImportUnrealStyleProof.py` consumes manifest and asserts destination exactly `/Game/Visual/StyleProof/USP_v001`. Assets live only below revisioned Meshes/Textures/Materials. Create opaque Default Lit master; base color near-uniform, metallic 1 on steel, roughness control .30/.23 by source slot, normal strength 0/1 through normalize(lerp(flat_tangent_normal, decoded_texture_normal, strength)). This bake already contains underlying surface normal and authored deviation: do not compose it with itself. Separate control, normal-only and normal+roughness instances. Preserve dark/padding slots independently.

Create/save `/Game/Visual/StyleProof/USP_v001/L_HelmetStyleProof` through LevelEditorSubsystem.new_level/save_current_level and EditorActorSubsystem. Map WorldSettings.default_game_mode=GameModeBase prevents CombatLabGameMode spawning unrelated gameplay scene. Movable shadowed directional key, movable SkyLight, warm ground/wall and dark opening stage; second neutral rig is switchable using tagged lights/stage materials. Fixed exposure/white balance, perspective camera, no DOF/motion blur. Reflection capture/skylight tuning remains measured work, not assumed equivalent to Riot shader.

Competitive is applied explicitly from Config/GraphicsProfiles.ini. TournamentGraphics.Initialize defaults to High; -LabProfile=Competitive alone is insufficient when custom GameModeBase bypasses its startup. Apply INI CVars in process after world exists and record SystemLibrary.get_console_variable_string_value for every requested CVar. Keep TAA=2, SSR=0, GI=0, reflection method=0. No global config changes.

## Capture mechanism and evidence

Installed UE5.8 declarations/source inspected, not yet execution-tested by this architecture step:

- PythonScriptPlugin EditorPythonExecuter.cpp supports `-ExecutePythonScript=` after editor/asset registry readiness; EditorPythonScripting.set_keep_python_script_alive(True) keeps asynchronous script alive. PySlate.cpp exposes register/unregister_slate_post_tick_callback.
- AutomationBlueprintFunctionLibrary.cpp:1223 implements AutomationLibrary.take_high_res_screenshot and binds its camera by editor viewport piloting. It returns AutomationEditorTask; wait for completion then file verification. This is an **editor** still, not gameplay/motion acceptance.
- LevelEditorSubsystem.cpp:265 implements editor_request_begin_play as PlayInEditor in active viewport. UnrealEditorSubsystem exposes get_game_world. Find copied actors by tag, get player controller, set_view_target_with_blend to proof camera.
- GameViewportClient.cpp:4284 handles `Shot filename="absolute/path.png" -nosuffix` through FScreenshotRequest at actual viewport resolution; preserves ordinary temporal rendering and excludes UI unless showui requested. Use this for PIE stills and motion image sequences, not high-resolution tiled screenshots.

Capture script is a nonblocking Slate-tick state machine: load map -> apply settings -> settle shader/streaming -> editor baseline if needed -> PIE -> obtain game world/controller -> set camera/rig/material -> warm up -> request one Shot -> wait for new complete output -> advance. Fail/quit on bounded timeout; release callback and keep-alive. Do not sleep on game thread, enqueue screenshots in a loop, or quit immediately after requesting one. Check PNG dimensions and nonblank pixels before receipt marks success. Use actual frame counters/fixed timestep for motion, and record each actor/light transform. A frame sequence with held poses is diagnostic only; continuous consecutive rendered frames are required for shimmer judgment.

Required initial stills: identical-camera daylight and neutral control/normal-only/combined; matched, front, three-quarter views. Motion: camera fixed, helmet turns; helmet fixed, key sweeps; transition into shade and gameplay pixel size. Store output and effective settings under Saved/ArtReview/UnrealStyle/USP_v001, distinguish editor vs PIE in every receipt.

## Risks / remaining gates

Runtime execution must confirm Python class spelling, editor viewport availability, screenshot completion and dimensions. Compression/tangent handedness needs directional-light and seam inspection. Recapture SkyLight only after rig changes, then settle; no hidden Lumen dependency. Editor motion tests do not prove shipped-frame performance. No broad benchmark until visual acceptance or a suspected slowdown; unchanged hardware/settings later required. Geometry reference match and portability remain separate gates from first material mechanism proof.

## Execution checkpoint

Source and scripts implemented. First full-editor attempt imported textures/material successfully but exposed a legacy FBX naming trap: `FillCollisionModelList` in FbxStaticMeshImport.cpp:2709 classifies any node containing `USP` as collision. Exported object/data renamed `SM_Helmet_StyleProof001`, with unchanged geometry/maps and refreshed FBX hashes; revision folder stays USP_v001.

`Tools/UnrealStyleProof.ps1 -Mode import` builds/saves the isolated map. `-Mode capture` captures initial stills into a fresh run_### directory; `-Mode capture -Full` includes motion. `-Mode open` opens the saved map. Launcher supplies -usefixedtimestep -fps=30. Capture records SystemLibrary.get_frame_count (GFrameCounter) at request/completion, effective CVars and Slate deltas; sequential frame IDs, not callback assumptions, govern the continuous motion receipt. Neutral mode switches stage materials too. Runtime validation remains in progress; consult import_receipt.json and capture_receipt.json for actual outcomes.

Runtime validation now confirms UE5.8.2 import height exactly35cm; all4 material slots and source maps persisted. PIE screenshot mechanism succeeds, actual viewport1525x870 with square870x870 camera framing; requested -ResX/-ResY does not resize embedded PIE. Game world delta is 1/30 second. Run001 rejected for DefaultPawn obstruction; run002 stopped on Python alias mismatch; run003 is unobstructed rendered evidence but its intended directional-light orientation was wrong. Python Rotator positional construction is unsafe here: all code now uses pitch/yaw/roll keyword arguments. Run004 stopped before pixels because the sky UI label does not match its property: installed SkyLightComponent.bLowerHemisphereIsBlack is exposed as lower_hemisphere_is_black. These diagnostics remain preserved; current receipt identifies later corrected runs.

Scoped revision and environment support: `-Revision USP_v002` selects only that revision. `-Mode environment` imports Environment_v001 authored HDRs as TextureCube under that revision without touching mesh/materials; `-Mode capture -Environment authored` records one corrected built-in control followed by authored daylight control/normal/combined. Both disable solid lower-hemisphere replacement; authored sky tint is white and intensity3. Named captures carry effective key rotation, sky intensity and lower-hemisphere flag. Pending visual results do not imply acceptance.

Latest validated engine checkpoint: USP_v002 imported/saved at height35cm with4 source slots and control/normal/combined/matte variants. Normal graph links are asserted; material Normal input resolves to Normalize, and instance NormalStrength receipts are `[0,0,0,0]` for control/matte and `[1,1,0,0]` for normals/combined. Source map/tangent import stayed consistent. Actual authored-environment evidence is USP_v001/run_005 and USP_v002/run_001. Both sets are ordinary PIE screenshots with Competitive CVars; v002 adds matte geometry and zero-direct-specular diagnostic. Zero direct specular removes the left bright forehead block while a right environment block remains, identifying separate direct and environment contributions. No fidelity pass or motion gate has been claimed.

Launcher additionally accepts `-KeySpecular 0..1` using installed LightComponent.SetSpecularScale; normal default1 preserves physical baseline. `-Mode open -Revision USP_v002 -Environment authored -Variant normals` runs OpenUnrealStyleProof.py and pilots the saved comparison camera. All novel engine fields were checked against installed headers after alias problems; avoid another inferred Python spelling. Captures are immutable run folders and map/assets are revision-scoped. No C++ or global render configuration edits were needed.


Current checkpoint (USP_v003): run_001 provides geometry/matte/native views under Environment_v002; run_002 isolates Environment_v003's lower ramp; run_003 isolates corrected cubemap orientation and the18-degree camera. Both no-pawn Blueprint GameMode and CameraPlayer0 autoactivation are verified in actual PIE before capture overrides. Every capture retains effective cubemap path, angle, key specular scale, camera transform/profile and world frame timing. No artist/motion pass has been claimed.

Cubemap primary-source validation: TextureCompressorModule.cpp:2182 and TransformSideToWorldSpace map cube+X to source panorama azimuth+90 degrees, cube+Y to180, and cube+Z to north. ReflectionEnvironmentShaders.usf:202 rotates the source sample vector by positive SourceCubemapAngle; ReflectionEnvironmentCapture.cpp:2860 converts that angle directly to radians. Thus source azimuth = engine yaw + SourceCubemapAngle +90 degrees. Setting SourceCubemapAngle=-90 restores the generator's declared yaw convention. This is source-derived mapping; actual captures verify the selected asset and applied-90 angle. HDR source files remain unchanged.

Additional scoped launcher controls: -EnvironmentRevision Environment_vNNN uses distinct texture asset suffixes; -CaptureSet environment_compare/environment_step/geometry/orientation_camera selects bounded experiments; -KeySpecularScale (legacy alias KeySpecular), -CubemapAngle, and -CameraProfile baseline/reference18 reproduce measured settings. -Mode save prepares a persisted winning map and winning_state.json using the same opener configuration; run it only after the artist gate identifies the winning settings. Full motion remains deferred until still acceptance.
