# Discarded project items — 7 September 2026

The cleanup and character agents aligned in CLEANUP_AGENT_ALIGNMENT.md. CF_v001 and its tools, runtime dependencies, source/config, Content and installed art/video dependencies are protected. The character agent has finished its delivery; no pose tests are requested by this cleanup.

Removed from active project folders after a verified recovery archive: pre-RC_v008 candidates and duplicate FP candidate export, obsolete Cascadeur probes/backups, isolated arm repair/refinement scripts, old review comparison/split/package tools, patch proposals and superseded Astra/Cascadeur-factory/right-cut handoff briefs. The full exact paths, original sizes and SHA-256 hashes are in the external manifest.

Recovery folder: C:/Users/Daniel Pang/Documents/Codex/2026-09-07/produce-a-comprehensive-audit-and-report/cleanup

- discarded-project-items.zip: original removed files and previous versions of reconciled documents, preserving project-relative paths.
- manifest.json: original sizes and SHA-256 identities; archive contents were verified before removal.
- plan.json and result.json: exact scope and actual execution result.

RC_v008's selected runtime bundle and main legacy export kit remain to keep the existing lab usable. No gameplay/runtime source, Config tracks, Content, new-character files, build outputs or installed dependencies were removed. They are not constraints on the new implementation. Retire those runtime dependencies only through a working replacement and call-site cleanup.

Current direction: CF_v001 review → whole exchange (MEL-6) → minimal new-rig playback (MEL-15) → human playable decision (MEL-11). The old-mesh shoulder repair MEL-14 is canceled as superseded, not reported fixed. Existing approved art direction is deferred, not discarded. Speed is key; further pose matrices and broad diagnostics are not mandatory gates.

The full audit and its frozen source/test evidence remain in the audit workspace. Historical documents may describe removed candidate paths; restore from the archive if historical reproduction is needed.
